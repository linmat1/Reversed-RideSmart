version: 1.0
runtime: python311
build:
  commands:
    pre_build:
      - pip install --upgrade pip
    build:
      - pip install -r requirements.txt
run:
  command: gunicorn -w 4 -b 0.0.0.0:8080 api:app
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"
